import { OpenInNewWindowIcon as RadixOpenInNewWindowIcon } from "@radix-ui/react-icons";
export const OpenInNewWindowIcon = ({
  width = "18",
  height = "18",
}: {
  width?: string;
  height?: string;
}) => {
  return <RadixOpenInNewWindowIcon width={width} height={height} />;
};
